{
  "name": "uml-flow",
  "private": true,
  "version": "1.2.0",
  "packageManager": "pnpm@10.33.3",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -w --filter @mduml/core run build && pnpm -w --filter @mduml/renderer-plantuml run build && pnpm -w --filter @mduml/runtime-mermaid run build && pnpm -w --filter @mduml/adapter-markdown-it run build && pnpm -w --filter @mduml/adapter-vscode run build && pnpm -w --filter @mduml/adapter-obsidian run build",
    "clean": "pnpm -r run clean",
    "test": "pnpm -w --filter @mduml/core run test && pnpm -w --filter @mduml/renderer-plantuml run test && pnpm -w --filter @mduml/runtime-mermaid run test && pnpm -w --filter @mduml/adapter-markdown-it run test",
    "demo": "node demo/serve.mjs",
    "release:tag": "pnpm exec semantic-release -e ./release.tag.cjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@semantic-release/commit-analyzer": "^13.0.1",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^11.0.1",
    "@semantic-release/npm": "^12.0.1",
    "@semantic-release/release-notes-generator": "^14.0.3",
    "rimraf": "^6.0.1",
    "semantic-release": "^24.2.7",
    "tsup": "^8.3.0",
    "typescript": "^5.6.3"
  }
}
